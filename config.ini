[models]
# Change this one line to switch all models at once
default = google/gemini-2.5-flash
# default = deepseek-v4-flash  # DeepSeek direct

profile_analysis = %(default)s
guide_selection  = %(default)s
llsettext        = %(default)s

# Lightweight eval — kept separate to avoid rate-limiting main dialogue
eval = google/gemma-3-4b-it:free
# eval = deepseek-v4-flash  # DeepSeek direct
